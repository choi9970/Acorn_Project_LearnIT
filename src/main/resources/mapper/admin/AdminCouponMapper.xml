<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.learnit.learnit.admin.AdminCouponMapper">

    <!--쿠폰 목록 조회-->
    <select id="selectCouponList" resultType="com.learnit.learnit.admin.AdminCouponDTO">
        SELECT
            coupon_id AS couponId,
            name,
            type,
            discount_amount AS discountAmount,
            min_price AS minPrice,
            expire_date AS expireDate,
            created_at AS createdAt
        FROM coupon
        ORDER BY created_at DESC
    </select>

    <!--쿠폰 생성-->
    <insert id="insertCoupon" parameterType="com.learnit.learnit.admin.AdminCouponDTO">
        INSERT INTO coupon (name, type, discount_amount, min_price, expire_date)
        VALUES ( #{name}, #{type}, #{discountAmount}, #{minPrice}, #{expireDate})
    </insert>


    <!--회원 검색-->
    <select id="searchUsers" resultType="com.learnit.learnit.user.dto.UserDTO">

        SELECT
            user_id AS userId,
            email,
            name,
            nickname,
            phone,
            status
        FROM users
        WHERE status = 'ACTIVE'
        <if test='keyword != null and keyword != ""'>
            AND (
                name LIKE CONCAT('%', #{keyword}, '%')
                OR email LIKE CONCAT('%', #{keyword}, '%')
                OR phone LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        ORDER BY created_at DESC
    </select>

</mapper>